import React from 'react'
import { Title } from './Title'

const Loading = () => {
    return (
        <section>
            <Title>Loading...</Title>
        </section>
    )
}

export default Loading